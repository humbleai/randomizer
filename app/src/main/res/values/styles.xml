<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorMainBg</item>
        <item name="colorPrimaryDark">@color/colorMainBg</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="elevation">1dp</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="elevation">1dp</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorControlNormal">@color/colorMainBg</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="overlayText">
        <item name="android:textColor">@color/colorAppCaption</item>
        <item name="android:textSize">42sp</item>
        <item name="android:shadowColor">@color/color_card_bg</item>
        <item name="android:shadowDx">3</item>
        <item name="android:shadowDy">3</item>
        <item name="android:shadowRadius">0.1</item>
    </style>

</resources>
